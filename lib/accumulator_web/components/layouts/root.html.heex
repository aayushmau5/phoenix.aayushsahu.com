<!DOCTYPE html>
<html lang="en" style="scrollbar-gutter: stable;">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title suffix=" · phoenix.aayushsahu.com">
      <%= assigns[:page_title] || "Home" %>
    </.live_title>
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>
  </head>
  <body class="bg-slate-900 text-white antialiased">
    <div class="flex bg-slate-500 m-2 rounded-md bg-opacity-10 p-2 justify-between items-center">
      <div class="flex items-center gap-2">
        <h1 class="font-bold">
          phoenix.aayushsahu.com
        </h1>
        <.link :if={@current_user} navigate={~p"/bin"} class="hover:bg-slate-800 p-2 rounded-md">
          /bin
        </.link>
      </div>
      <ul class="text-right">
        <%= if @current_user do %>
          <li class="text-white">
            <%= @current_user.email %>
          </li>
          <li>
            <.link
              href={~p"/logout"}
              method="delete"
              class="text-white font-semibold hover:text-sky-300"
            >
              Log out
            </.link>
          </li>
        <% else %>
          <li>
            <.link href={~p"/login"} class="text-white font-semibold hover:text-sky-300">
              Log in
            </.link>
          </li>
        <% end %>
      </ul>
    </div>

    <%= @inner_content %>
  </body>
</html>
